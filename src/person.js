export const ifAdult = (age) => age >= 18 ;
export const canDrink = (age) =>  age >= 21;
// const isSeniorCitizen = (age) => age >= 60;
// export default isSeniorCitizen;

export default (age) => age >=55;
   